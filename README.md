# PMAAS Basic Web UI

Provides a basic web-based user interface for a PMAAS assembly.  Uses templates, so it requires the assembly to include a plugin that that 
implements `spi.IPMAASTemplateEnginePlugin`, such as https://github.com/avanha/pmaas-plugin-golangtextteplate.
